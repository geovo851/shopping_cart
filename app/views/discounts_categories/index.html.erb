<% provide(:title, 'Discounts on categories') %>

<div class="container-fluid"> 
  <div class="row">
    <div class="col-sm-10 col-md-10">

      <div class="page-header">
        <h1>Discounts on categories</h1>
      </div>
      <p>
        <%= button_to new_discounts_category_path, method: :get, class: 'btn btn-primary' do %>
          <span class="glyphicon glyphicon-plus"></span>
          Add discount on category
        <% end %>
      </p>

      <% if @discounts_categories.empty? %>
        <b>No discounts on categories</b>
      <% else %>

      <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
          <thead>
            <tr class="warning">
              <th>Id</th>
              <th>Category</th>
              <th>Quantity of purchased products</th>
              <th>Percentage or sum discount</th>
              <th>Type of discounts</th>
              <th>Actual</th>
              <th colspan="2"></th>
            </tr>
          </thead>
          <tbody id="discounts-categories">
            <%= render partial: "discounts_category", :collection => @discounts_categories, as: :discounts_category %>
          </tbody>
        </table>
      </div>
      <div id="paginator", class="paginate-centre">
        <%= paginate @discounts_categories, :remote => true %>
      </div>
      <% end %>

      <p>
        <%= button_to 'Back', admin_path, method: :get, class: "btn btn-default" %>
      </p>
    </div>
  </div>
</div>
